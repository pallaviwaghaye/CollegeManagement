<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.webakruti.collegemanagement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/NoActionBar">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".LandingActivity" />
        <activity android:name=".Student.RegistrationActivity" />
        <activity android:name=".Student.LoginActivity" />
        <activity android:name=".Student.HomeStudentActivity" />
        <activity android:name=".Admin.AdminLandingActivity" />
        <activity android:name=".Admin.AdminLoginActivity" />
        <activity android:name=".Admin.AdminRegistrationActivity" />
        <activity android:name=".Admin.AdminHomeActivity" />
        <activity android:name=".Student.MyProfileActivity" />
        <activity android:name=".Student.AcademicDetailsActivity" />
        <activity android:name=".Student.AttendanceActivity" />
        <activity android:name=".Student.ResultActivity" />
        <activity android:name=".Student.ExamScheduleActivity"></activity>
    </application>

</manifest>